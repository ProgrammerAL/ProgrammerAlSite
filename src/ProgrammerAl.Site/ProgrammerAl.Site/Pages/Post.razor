@page "/posts/{PostUrl}"
@using System.Text.Encodings.Web;

@inherits ProgrammerAl.Site.PageModels.BlogPostModel

<HeadContent>
    @* <meta name="og:title" content="ProgrammerAL.com" /> *@
</HeadContent>

@if (!string.IsNullOrWhiteSpace(PostHtml.Value))
{
    @PostHtml
}
